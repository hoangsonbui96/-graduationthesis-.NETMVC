@model IEnumerable<MyStore.Models.RoleMaster>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoleCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
